<form method="post" action="{{ path('question_delete', {'id': lesson.id}) }}" onsubmit="return confirm('Вы уверены, что хотите удалить вопрос?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question.id) }}">
    <button class="btn">Delete</button>
</form>
