<form method="post" action="{{ path('pass_again_course', {'courseId': course.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('pass_again' ~ course.id) }}">
    <button class="btn btn-primary my-2">{{ button_label|default('Пройти сначала') }}</button>
</form>