{% extends 'base.html.twig' %}

{% block title %}Главная страница{% endblock %}

{% block body %}
    <div class="container text-center py-5 bg-light ">
        <p>
            <a href="{{ path('course_new') }}" class="btn btn-primary my-2">Создать курс</a>
            <a href="{{ path('my_courses_teacher')}}" class="btn btn-secondary my-2">Мои курсы</a>
        </p>

    </div>

{% endblock %}
